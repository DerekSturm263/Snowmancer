using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Elements : MonoBehaviour
{
    public enum Element { fire, ice, earth, wind }

    //void ThrowSnowball(Element type, 
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
